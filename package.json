{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "install:all": "npm install --prefix ecommerce-app/server && npm install --prefix ecommerce-app/client",
    "server": "npm run dev --prefix ecommerce-app/server",
    "client": "npm start --prefix ecommerce-app/client",
    "dev": "npm run server",
    "start": "npm run server",
    "lint": "echo \"No lint configured\""
  }
}
