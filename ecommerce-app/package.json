{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "install:all": "npm install --prefix server",
    "server": "npm run dev --prefix server",
    "dev": "npm run server" ,
    "lint": "echo \"No lint configured\""
  }
}
